# Dossier technique – Mini-site web SISR

Complétez ce modèle avant de l'exporter en PDF. Remplacez `NomEtudiant` et les éléments entre crochets par vos informations.

## 1. Informations générales
- **Étudiant·e :** Nom Prénom
- **Classe :** SIO11 ou SIO12
- **Intitulé du projet :** Mini-site web SISR
- **Nom du fichier PDF remis :** `AP-<classe>-<nom>-html-css.pdf`

## 2. Corrections et améliorations
Pour chaque correction apportée au HTML/CSS, insérez :
1. Une capture d'écran annotée (à intégrer lors de l'export PDF).
2. Une description concise de la correction réalisée.

| Capture | Description de la correction | Impact |
| --- | --- | --- |
| (à compléter) | (à compléter) | (accessibilité, lisibilité, conformité, etc.) |

## 3. Mémento des commandes Linux
- Capture de la table `linux_command_line.html`.
- Listez les balises principales utilisées (`<table>`, `<thead>`, `<tbody>`, `<th>`, `<td>`, `<caption>`).
- Expliquez leur rôle dans un paragraphe.

## 4. Formulaire du footer
- Capture du formulaire commun placé dans le footer.
- Balises principales : `<form>`, `<label>`, `<input>`, `<textarea>`, `<button>`.
- Décrivez le rôle de chaque élément et l'expérience utilisateur visée.

## 5. Attributs des formulaires
Expliquez les attributs suivants avec un exemple issu de votre code :
- **`method`** : type de requête utilisée pour envoyer le formulaire (`get` ou `post`).
- **`action`** : URL vers laquelle les données sont envoyées.
- **`required`** : rend la saisie obligatoire pour valider le formulaire.
- **`name`** : identifiant du champ côté serveur pour récupérer la valeur.

## 6. Liaison HTML / CSS
Décrivez comment vous reliez un fichier CSS à un fichier HTML :
```html
<link rel="stylesheet" href="css/Global.css" />
```
Expliquez pourquoi cette méthode (fichier CSS externe) facilite la maintenance, la réutilisation et la cohérence visuelle du site.

## 7. Pages du mini-site
Ajoutez des captures d'écran des pages suivantes et décrivez leur contenu :
- `index.html`
- `pages/html.html`
- `pages/css.html`
- `pages/linux_command_line.html`
- `pages/en_construction.html`
- `pages/dossier_remise.html`

> 💡 **Astuce :** conservez les captures sur votre ordinateur et intégrez-les dans le PDF final uniquement. Ne les ajoutez pas dans le dépôt Git.

## 8. Lien vers le dépôt
- URL du dépôt Git (optionnel si le projet est livré par archive uniquement).

## 9. Historique des livrables
- Nom de l'archive ZIP remise : `AP-<classe>-<nom>-mini-site.zip`.
- Date d'envoi sur l'ENT et sur la plateforme de dépôt.

---
**Signature de l'étudiant·e :**

